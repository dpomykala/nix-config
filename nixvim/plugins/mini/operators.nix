_: {
  plugins.mini.modules.operators = {
    # The `gs` prefix conflicts with custom mappings for mini.surround
    sort.prefix = "g\\";
    # TODO: Replace gO/go with built-in keymaps [<Space> and ]<Space> ?
  };
}
